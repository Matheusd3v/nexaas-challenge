import styled from "styled-components";

export const SecondayContainer = styled.div`
    max-width: 1800px;
    width: 100%;
    height: calc(100vh - 80px);
    margin: 0 auto;
    display: flex;
`

export const ContentContainer = styled.section`
    width: 100%;
    height: 100%;
    display: flex;
    flex-direction: column;
    align-items: flex-start;
`